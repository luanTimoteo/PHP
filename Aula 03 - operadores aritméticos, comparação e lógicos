<body>
    <?php
        //operadores aritmeticos
        // + para soma, - para subtração, * para multiplicação, / para divisão, % para resto de divisão, ** para exponencial
        echo (1+8)*3;
        
        //para usar o hr que é uma tag html dentro do php precisar usar dessa forma
        echo '<hr/>';

        //aqui criei as variaveis 
        $var = 'boa noite';
        $nome = 'meu nome é Luan';
        
        //aqui usa o . junto das variaveis para concatenar elas e juntar as strings
        $saudacao = $var.', '.$nome;
        echo $saudacao;

        //quebra de linha
        echo '<hr/>';

        //declarei variaveis com numeros para realizar calculos
        $a=1;
        $a=$a + 1;
        echo $a;

        //quebra de linha
        echo '<hr/>';

        //mais uma declaração de variavel com soma porem escrito de outra forma
        $b=1;
        $b+=1;
        echo $b;

        //quebra de linha
        echo '<hr/>';

        /*operadores de comparação para saber se algo é verdadeiro ou falso
        Uma vez o = é para atribuir e duas vezes o == é para comparação
        $X==$Y;
        
        3 vezes o === é para comparar tambem se o tipo de dado é igual, string, int etc...
        $x===$y;

        para comparar e verificar se é diferente
        $x<>$y;

        aqui ta perguntando x não é igual a y
        $x!==$y;

        aqui comparadores mais padrões começa com menor que maior que depois menor que ou igual e depois maior que ou igual
        vai retornar true ou false
        $x<$y;
        $x>$y;
        $x<=$y;
        $x>=$y;
        */
    
        //aqui vai um exemplo de comparação de variaveis
        $a=1;
        $b=1;
        $c=2;
        var_dump($a==$b);

        //quebra de linha
        echo '<hr/>';
        var_dump($a<>$b);
        //quebra de linha
        echo '<hr/>';
        var_dump($b<$c);
        //quebra de linha
        echo '<hr/>';
        var_dump($b>=$c);
        //quebra de linha
        echo '<hr/>';

        //Agora vamos para os operadores lógicos AND ou &&, OR ou ||, XOR
        $a= FALSE;
        $B= true;
        $C= TRUE;
        //aqui ta perguntando se é verdadeiro no AND todos tem que ser TRUE para retornar verdadeiro
        var_dump($a && $b && $c);
        var_dump($b && $c);

        //no OR ou || uma delas tem que ser TRUE se uma for TRUE ele retorna TRUE
        var_dump($a OR $b);

        //XOR ele é parecido com OR, vai comparar um valor com outro para ver se são verdadeiros mas não ambos
        var_dump($a XOR $b);
        var_dump($b XOR $c);

    ?>
</body>
